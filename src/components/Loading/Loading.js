import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center'>
            <button className="btn loading mx-auto mt-36">loading</button>
        </div>
    );
};

export default Loading;